name: gerador_pix
description: Um gerador de QR Code PIX Estático em Flutter.
version: 1.0.0+1

environment:
  sdk: '>=3.11.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  qr_flutter: ^4.1.0
  shared_preferences: ^2.5.0
  lucide_icons: ^0.257.0
  intl: ^0.20.0
  uuid: ^4.5.0
  share_plus: ^12.0.0
  path_provider: ^2.1.0
  mobile_scanner: ^7.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
